<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.bme.ttk.lijia8.rgbledcontroller">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".RGBApplication"
        android:allowBackup="true"
        android:icon="@drawable/color_wheel_730"
        android:label="@string/app_name"
        android:roundIcon="@drawable/color_wheel_730"
        android:supportsRtl="true"
        android:theme="@style/Theme.RGBLEDController">
        <activity
            android:name=".MainSettingsActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>